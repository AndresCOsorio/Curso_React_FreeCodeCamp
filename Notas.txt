Curso React js
Conceptos importantes
Componente: Parte de la interfaz de usuario que es independiente y reusable(Tipos funcionales y de Clase).
	Funcionales: Funcion de javaScript/ES6 que retorna un elemento de React(JSX).
		Caracteristicas: Debe retornar un elemento de React(JSX).
						 Debe comenzar con una letra mayúscula.
						 Puede recibir valores si es necesario.
	Clase: Clase de javaScript/ES6 que retorna un elemento de React(JSX).
		Caracteristicas: Debe exteder React.Component.
						 Debe tener un metodo render para retornar un elemento de React(JSX).
						 Puede recibir valores si es necesario.
Props: Argumentos que puede recibir un componente de React. Solo pueden ser enviados de padre a hijo.
Estado: Representación en javaScript del conjunto de propiedades(Información que se representa sobre el componente) de un componente y sus valores actuales .
Hooks: Funcion especial que te permite trabajar con estados en componentes funcionales y otros aspectos de React.
Event listener(Event Handler): Funcion en JavaScript que es ejecutada cuando ocurre un evento especifico.

crear aplicación base react
npx create-react-app

Los archivos de react pueden ser extension js o jsx

Elementos aprendidos de React.
props
props.children


Proyecto 1:
Retos:
	1. Implementar texto en negrita qe se encuentra en el testimonio
	2. cambiar valor de alt imagen que sea dinamico.
	3. Tener archivo aparte para tomar los testimonios

Proyecto 2:
	1.	Ser consistente con los puntos y coma en el codiho
	2.	Retirar el modulo de importacion React

Proyecto 3:
Comentarios:

Retos:
	1. Crear componente para el logo de freeCodeCamp.
	2. Detectar y corregir errores que se generen en la aplicación.
